const root=document.documentElement;const themeBtn=document.querySelector('#themeBtn');const stored=localStorage.getItem('theme');if(stored)root.dataset.theme=stored;themeBtn.textContent=root.dataset.theme==='dark'?'☀':'☾';themeBtn.onclick=()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('theme',root.dataset.theme);themeBtn.textContent=root.dataset.theme==='dark'?'☀':'☾'};
const toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('#nav');toggle.onclick=()=>{nav.classList.toggle('open');toggle.setAttribute('aria-expanded',nav.classList.contains('open'))};nav.querySelectorAll('a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
const words=['digital solutions.','measurable improvements.','scalable systems.','sustainable change.'];let wi=0,ci=0,deleting=false;const typed=document.querySelector('#typed');function type(){const w=words[wi];typed.textContent=w.slice(0,ci);if(!deleting&&ci<w.length)ci++;else if(deleting&&ci>0)ci--;else{deleting=!deleting;if(!deleting)wi=(wi+1)%words.length}setTimeout(type,deleting?45:ci===w.length?1200:80)}type();
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
let counted=false;const impact=document.querySelector('#impact');new IntersectionObserver(es=>{if(es[0].isIntersecting&&!counted){counted=true;document.querySelectorAll('[data-count]').forEach(el=>{let end=+el.dataset.count,start=0,steps=45,i=0;const timer=setInterval(()=>{i++;let n=end*(i/steps);el.textContent=(el.dataset.prefix||'')+(end%1?n.toFixed(1):Math.round(n))+(el.dataset.suffix||'');if(i>=steps)clearInterval(timer)},25)})}},{threshold:.3}).observe(impact);
document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelector('.filters .active').classList.remove('active');b.classList.add('active');document.querySelectorAll('.project-card').forEach(c=>c.classList.toggle('hidden',b.dataset.filter!=='all'&&c.dataset.category!==b.dataset.filter))});
const modal=document.querySelector('#modal');document.querySelectorAll('.details').forEach(b=>b.onclick=()=>{document.querySelector('#modalTitle').textContent=b.dataset.title;document.querySelector('#modalText').textContent=b.dataset.text;modal.classList.add('open')});document.querySelector('#modalClose').onclick=()=>modal.classList.remove('open');modal.onclick=e=>{if(e.target===modal)modal.classList.remove('open')};document.addEventListener('keydown',e=>{if(e.key==='Escape')modal.classList.remove('open')});document.querySelector('#year').textContent=new Date().getFullYear();